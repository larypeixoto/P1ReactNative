import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 0,
  },

  list: {
    padding: 4,
    paddingHorizontal: 10,
    alignItems: 'center',
  },

  row: {
    marginBottom: 10,
    gap: 5,
    marginLeft: 10,
  },

  title: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 10,
  },
});
